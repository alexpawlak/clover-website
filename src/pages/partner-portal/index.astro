---
export const prerender = false;

import { requireAuth } from '../../lib/auth';

const { redirect } = await requireAuth(Astro.cookies);
if (redirect) return Astro.redirect(redirect);

return Astro.redirect('/partner-portal/offers');
---
